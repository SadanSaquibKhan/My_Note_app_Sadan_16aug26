# Junk that Google Drive / Windows drop into this folder
desktop.ini
Thumbs.db
*.tmp
~$*

# Working documents that live in this folder but must not be published.
# This repo is a public GitHub Pages site: anything committed here is on the
# open web. Only the app itself belongs in it.
*.docx
*.doc
*.pdf
v11_device_test_results_and_tablet_issues.md

# Device password for the sync server. Never publish.
margin-sync-key.txt

# Test-only dependencies. tests/peek.mjs needs linkedom to give it a DOM;
# install it with `npm install --no-save linkedom`. It must never be committed:
# this repo is a public GitHub Pages site and only the app belongs in it.
node_modules/
