{
  "name": "sync-server",
  "version": "1.0.0",
  "description": "The small always-on service the app talks to, to sync **light data only** — notes, sections, notebooks, ink strokes, settings. It never handles audio. See `../SYNC-PLAN.md` for the why.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
